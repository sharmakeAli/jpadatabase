server:
  port: 6060
spring:
  application:
    name: JPA-Database
  datasource:
    username: root
    password: abc123
    url: jdbc:postgresql://localhost:4432/just_test
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

